import { createStore } from 'vuex';

const store = createStore({
  state: {
    // 控制页脚是否透明
    footerBlur: true,
    // 控制盒子是否显示
    boxOpenState:false
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },

});

export default store;
